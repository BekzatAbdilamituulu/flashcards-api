# Frontend

Vite React client for DeepLex.

## Environment

Copy `.env.example` to `.env` and configure:

- `VITE_API_BASE_URL`
- `VITE_GOOGLE_CLIENT_ID`

`VITE_GOOGLE_CLIENT_ID` must be a Google OAuth web client ID configured for the frontend origin.
